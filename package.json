{
  "name": "gh-repo-config",
  "version": "0.7.1",
  "description": "Org-wide repo-configuration converger — deterministic core consumed as an immutable GitHub Release.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "gh-repo-config": "bin/gh-repo-config.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/**/*.test.js",
    "lint:md": "markdownlint-cli2 \"**/*.md\" \"#node_modules\" \"#dist\"",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "markdownlint-cli2": "0.23.2",
    "typescript": "5.9.3"
  }
}
