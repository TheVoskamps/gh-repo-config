# Memory Index

- [worktree gate refuses heredocs, `git -C`, and chains](worktree_gate_refuses_heredocs_and_chains.md) — any heredoc/redirect/loop/`git -C`/heredoc-commit is refused, git or not; Write a script or message file and run it as one static command (`bash <file>`, `git commit -F <file>`)
